{
  "name": "facturino-dashboard",
  "version": "1.0.0",
  "description": "Facturino dashboard",
  "main": "src/index.ts",
  "type": "module",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev"
  },
  "dependencies": {
    "@cloudflare/kv-asset-handler": "^0.3.1"
  },
  "devDependencies": {
    "wrangler": "^4.120.0",
    "@types/node": "^20.0.0"
  }
}
